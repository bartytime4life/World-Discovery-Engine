# FILE: .dockerignore
# -------------------------------------------------------------------------------------------------
# 🌍 World Discovery Engine (WDE) — Production Docker Ignore
# Strips everything except core source + configs to keep images lean and secure.
# -------------------------------------------------------------------------------------------------

# Python / Build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.manifest
*.spec

# Virtual environments
.venv*/
.venv/
venv/
env/
ENV/
.env
*.env.*
.envrc
poetry.lock

# Notebooks / Checkpoints
*.ipynb_checkpoints/
**/.ipynb_checkpoints/**
*.nbconvert.ipynb

# Data & artifacts (DVC-managed, never in prod image)
data/
artifacts/
outputs/
reports/
site/
logs/
tmp/
cache/
runs/

# DVC internals (only metadata tracked in Git, not needed in container)
.dvc/cache/
.dvc/tmp/
.dvc/config.local
.dvc/lock

# GIS spillover
*.aux.xml
*.ovr
*.tfw
*.prj
*.cpg
*.shx
*.shp.*
*.dbf
*.sidecar
*.worldfile

# Secrets
kaggle.json
*.key
*.pem
*.crt
*.cert
*.p12

# Logs & debug
*.log
*.out
*.err
profile_default/
ipython_config.py

# OS / Editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Docker / CI
docker-compose.override.yml
docker-compose.local.yml
.docker/
.dockerignore
